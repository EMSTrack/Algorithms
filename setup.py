# TODO This EMSTrack library may eventually become a lib that can be installed by pip.
# TODO If we write this setup.py script correctly, I think it'll allow the user to avoid installing
# TODO the lower level libraries like numpy. (Assuming our lib is a higher level lib)